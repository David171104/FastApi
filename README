<h1 align="center">
  🚀 Full Stack App with FastAPI & Svelte
</h1>

<p align="center">
  <img src="https://fastapi.tiangolo.com/img/logo-margin/logo-teal.png" alt="FastAPI Logo" width="160" />
  &nbsp;&nbsp;&nbsp;
  <img src="https://upload.wikimedia.org/wikipedia/commons/1/1b/Svelte_Logo.svg" alt="Svelte Logo" width="80" />
</p>

<p align="center">
  <b>Modern full-stack project using FastAPI (Python) for the backend and Svelte for the frontend.</b>
</p>

---

## 🧠 Overview

This project demonstrates how to build a complete CRUD system using **FastAPI** as the backend framework and **Svelte** as the frontend framework.

The backend handles authentication (JWT), user management, and database communication, while the frontend provides an elegant and interactive UI for consuming the API.

---



Guía de instalación y configuración
# 1. Descargar paquetes de Python
Asegúrate de tener Python instalado en tu sistema.

# 2. Crear un entorno virtual
En la terminal, ejecuta el siguiente comando para crear un entorno virtual:

**python -m venv myvenv**

Para activar el entorno virtual:

En Windows:

**.\myvenv\Scripts\activate**

En macOS/Linux:

**source myvenv/bin/activate**

# 3. Actualizar pip
Una vez activado el entorno virtual, actualiza pip:

**python -m pip install --upgrade pip**

# 4. Configurar .gitignore
Si estás utilizando Git (recomendado), crea un archivo .gitignore y agrega lo siguiente para excluir el entorno virtual del repositorio:

**myvenv/**


# 5. Instalar dependencias
Tienes dos opciones:

Usando requirements.txt:

**pip install -r requirements.txt**

Instalando manualmente:

**pip install "fastapi[standard]" uvicorn**

# 6. Ejecutar proyecto 


fastapi dev main.py

# 7. desactivar entorno

deactivate


# Intrucciones para ejecutar sveltekit 

npm run dev -- --open

# ejecutar ambos proyectos 
"start.bat"

# paquetes npm instalados:
axios
sweetalert2
